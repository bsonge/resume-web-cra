import { React } from "react";
import "./Footer.scss";

const Footer = (props) => {
    return(
        <div className="site-footer">
            Footer stuff
        </div>
    );
}

export default Footer;